<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wugui.datax.admin.mapper.JobUserRoleMapper">

	<delete id="deleteBatch">
		delete from job_user_role where role_id in
		<foreach item="roleId" collection="array" open="(" separator="," close=")">
			#{roleId}
		</foreach>
	</delete>

	<select id="queryRoleIdList" resultType="long">
		select role_id from job_user_role where user_id = #{userId}
	</select>
	<select id="queryRoleNames" resultType="java.lang.String">
		select t2.role_name from job_user_role t1
		left join job_role t2
		on t1.role_id = t2.role_id
		where t1.user_id = #{userId}
	</select>
</mapper>